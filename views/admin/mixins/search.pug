mixin search(keyWord)
    form(class="form-seacrh")
        .input-group
            input(type="text"
            placeholder="Nhập từ khoá"
            name="keyword"
            class="form-control"
            value=keyWord 
            )
            .input-group-append 
                button(
                class="btn btn-success btn-sm"
                type="submit"
                ) Tìm